/* */
//import { get1Element } from './get1Element.js';

export function changeHead() {
  console.log('The function "changeHeadText" is executed');
  /*
  const mainIndex = get1Element('mainIndex');
  const mainBookmarks = get1Element('mainBookmarks');
  const mainCreate = get1Element('mainCreate');
  const mainProfile = get1Element('mainProfile');

  const headText = get1Element('changeHeadText');

  if (!mainIndex.classList.contains('hide-it')) {
    headText.textContent = 'QUIZ APP';
  } else if (!mainBookmarks.classList.contains('hide-it')) {
    headText.textContent = 'BOOKMARKS';
  } else if (!mainCreate.classList.contains('hide-it')) {
    headText.textContent = 'CREATE';
  } else if (!mainProfile.classList.contains('hide-it')) {
    headText.textContent = 'PROFILE';
  } */
}
